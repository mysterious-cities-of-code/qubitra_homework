{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Client:Request:Meter:Query Usage Data",
  "description": "Represents an variable param client request for meter data",
  "type": "object",
  "properties": {
    "meter_id": {
      "type": "string"
    },
    "tariff_id": {
      "type": "string"
    },
    "from": {
      "type": "string",
      "format": "date-time"
    },
    "to": {
      "type": "string",
      "format": "date-time"
    },
    "sort": {
      "type": "string",
      "$comment": "Bool or string for meter id? If meter id, what if id provided differs from meter id provided in the query params; TBC"
    },
    "aggregate_by_meter": {
      "type": "boolean"
    }
  },
  "required": [
    "from",
    "to"
  ]
}